CREATE TABLE product (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nama VARCHAR(50) NOT NULL,
    harga VARCHAR(11) NOT NULL,
    stok INT (11) NOT NULL,
    foto VARCHAR(255) NOT NULL,
    typeCoffee TEXT(255) NOT NULL
);

